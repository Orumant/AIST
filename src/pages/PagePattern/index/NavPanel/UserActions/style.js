import React from 'react';

export const styles = theme => {
  return{
    accountIcon: {
      marginRight: theme.spacing.unit,
    },
    createButton: {
      border: '2px solid #fff',
    },
  }};

